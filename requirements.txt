# Runtime dependencies, pinned to the versions this project is tested against.
# Regenerate/refresh with: pip freeze | grep -iE '<pkg>'
#
# LangChain / LangGraph core stack (fast-moving; pinned exactly for reproducibility).
langchain==1.3.13
langchain-community==0.4.2
langchain-google-genai==4.2.7
langgraph==1.2.9

# Web search backend for the Researcher agent.
# langchain-community's DuckDuckGo tool now delegates to the `ddgs` package
# (the former `duckduckgo-search`, which it renamed).
ddgs==9.14.4

# Configuration & validation (pydantic is already required transitively;
# pydantic-settings provides the 12-factor env-driven Settings object).
pydantic==2.13.4
pydantic-settings==2.14.2
# Required by pydantic-settings to load the local .env file.
python-dotenv==1.2.2

# HTTP API surface (FastAPI + LangServe playground/routes).
fastapi==0.139.1
uvicorn==0.51.0
langserve[all]==0.3.3
