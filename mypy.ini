[mypy]
python_version = 3.10
# The pydantic plugin teaches mypy that BaseSettings fields are populated from
# the environment, so Settings() needs no explicit arguments.
plugins = pydantic.mypy
# Third-party packages in this stack ship without (or with partial) type stubs.
ignore_missing_imports = True
warn_unused_ignores = True
