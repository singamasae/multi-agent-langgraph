# Development-only dependencies (testing, linting, type checking).
# Install alongside runtime deps:
#   pip install -r requirements.txt -r requirements-dev.txt
-r requirements.txt

pytest==9.1.1
pytest-mock==3.15.1
# httpx powers FastAPI's TestClient used in the API integration test.
httpx==0.28.1
ruff==0.15.21
mypy==2.3.0
